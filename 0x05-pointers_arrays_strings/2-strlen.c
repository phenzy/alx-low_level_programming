#include "main.h"
/**
 * _strlen - check code.
 * @s: declaration of *s and paramters for the function _strlen
 * Return: 0.
 */
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}
	return (c);
}
